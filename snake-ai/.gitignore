__pycache__/
*.pyc
.venv/
venv/

# 학습된 체크포인트는 각자 로컬에서 생성되므로 버전관리에서 제외
# (디렉토리 구조 유지를 위해 .gitkeep만 추적)
model/*.pth

# train.py 실행마다 새로 생성되는 학습 로그(episode, score, mean_score, avg_loss)
training_log.csv
